{
  "name": "media-library-server",
  "version": "1.0.0",
  "description": "Home Media Library Server with React Frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build:react && npm run build:server",
    "build:react": "cd client && npm run build && cp -r build ../build",
    "build:server": "echo 'Server build complete'",
    "install:all": "npm install && cd client && npm install",
    "dev:server": "nodemon server.js",
    "dev:client": "cd client && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "lucide-react": "^0.525.0",
    "mime-types": "^2.1.35"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [
    "media",
    "library",
    "server",
    "react",
    "nodejs",
    "video",
    "streaming"
  ],
  "author": "Your Name",
  "license": "MIT"
}
